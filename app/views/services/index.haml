%section.hero
  .hero-body
    %h4.subtitle.is-4 Programa Servicio BX
    .columns.is-multiline
      - @services.each do |service|
        .column.is-4
          .card.is-shady
            .card-content
              .content
                %h4= service.name
                %a{:href => service.url} Ver más
