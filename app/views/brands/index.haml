%section.hero
  .hero-body
    .columns.is-multiline
      - @brands.each do |brand|
        .column.is-4
          .card.is-shady
            .card-content
              .content
                %h4= brand.name
                %a{:href => brand.url} Ver más
